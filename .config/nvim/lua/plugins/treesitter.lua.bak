return {
  "nvim-treesitter/nvim-treesitter",
  build = ":TSUpdate",
  opts = {
    ensure_installed = { "lua", "java", "markdown", "vim", "vimdoc" },
    autoinstall = true,
    highlight = { enabled = true },
    indent = true,
  },
}
